<?php
// customer/products/index.php - WITH COLLAPSIBLE SIDEBAR FOR MOBILE + ICONS
require_once '../../config/database.php';

session_start();

// Pagination
$page = isset($_GET['page']) ? (int)$_GET['page'] : 1;
$limit = 15;
$offset = ($page - 1) * $limit;

// Filters
$category_id = isset($_GET['category']) ? (int)$_GET['category'] : 0;
$search = isset($_GET['search']) ? trim($_GET['search']) : '';
$min_price = isset($_GET['min_price']) ? (int)$_GET['min_price'] : 0;
$max_price = isset($_GET['max_price']) ? (int)$_GET['max_price'] : 0;
$sort = isset($_GET['sort']) ? $_GET['sort'] : 'newest';

// Build WHERE clause
$where = "WHERE p.is_available = 1";

if ($category_id > 0) {
    $where .= " AND p.category_id = $category_id";
}

if (!empty($search)) {
    $search_escaped = mysqli_real_escape_string($conn, $search);
    $where .= " AND (p.name LIKE '%$search_escaped%' OR p.description LIKE '%$search_escaped%')";
}

if ($min_price > 0) {
    $where .= " AND p.price >= $min_price";
}

if ($max_price > 0) {
    $where .= " AND p.price <= $max_price";
}

// Sorting
$order_by = "ORDER BY p.created_at DESC";
if ($sort == 'price_low') $order_by = "ORDER BY p.price ASC";
if ($sort == 'price_high') $order_by = "ORDER BY p.price DESC";
if ($sort == 'popular') $order_by = "ORDER BY p.views DESC";
if ($sort == 'name_asc') $order_by = "ORDER BY p.name ASC";
if ($sort == 'name_desc') $order_by = "ORDER BY p.name DESC";

// Get total count
$count_sql = "SELECT COUNT(*) as total FROM products p $where";
$count_result = mysqli_query($conn, $count_sql);
$count_data = mysqli_fetch_assoc($count_result);
$total_products = $count_data['total'] ?? 0;
$total_pages = ceil($total_products / $limit);

// Get products
$sql = "SELECT p.*, b.business_name, b.location, c.name as category_name 
        FROM products p 
        JOIN businesses b ON p.business_id = b.business_id 
        JOIN categories c ON p.category_id = c.category_id 
        $where 
        $order_by 
        LIMIT $offset, $limit";
$products_result = mysqli_query($conn, $sql);
$products = [];
while ($row = mysqli_fetch_assoc($products_result)) {
    $products[] = $row;
}

// Build hierarchical category tree for sidebar
$all_categories = [];
$cat_sql = "SELECT * FROM categories ORDER BY parent_id, sort_order, name";
$cat_result = mysqli_query($conn, $cat_sql);
while ($row = mysqli_fetch_assoc($cat_result)) {
    $all_categories[] = $row;
}

function buildCategoryTree($categories, $parent_id = NULL) {
    $branch = [];
    foreach ($categories as $cat) {
        if ($cat['parent_id'] == $parent_id) {
            $children = buildCategoryTree($categories, $cat['category_id']);
            $cat['children'] = $children;
            $branch[] = $cat;
        }
    }
    return $branch;
}

$category_tree = buildCategoryTree($all_categories);

// Function to display categories: main categories togglable (static > icon), subcategories filterable
function displayCategoryTree($tree, $current_category_id, $level = 0) {
    $html = '';
    foreach ($tree as $cat) {
        $has_children = !empty($cat['children']);
        $is_active = ($current_category_id == $cat['category_id']);
        
        $html .= '<li class="category-item">';
        
        if ($has_children) {
            // Main category: click toggles subcategories, static '>' icon
            $html .= '<div class="category-link-wrapper">';
            $html .= '<a href="javascript:void(0)" class="category-link main-category" onclick="toggleSubcategories(this)">';
            $html .= '<span class="category-name"><i class="fas fa-folder"></i> ' . htmlspecialchars($cat['name']) . '</span>';
            $html .= '<span class="category-toggle-icon"><i class="fas fa-chevron-right"></i></span>';
            $html .= '</a>';
            $html .= '</div>';
            // Subcategories container (initially hidden)
            $html .= '<ul class="subcategory-list" style="display: none;">';
            $html .= displayCategoryTree($cat['children'], $current_category_id, $level + 1);
            $html .= '</ul>';
        } else {
            // Subcategory: filters products
            $html .= '<div class="category-link-wrapper">';
            $html .= '<a href="?category=' . $cat['category_id'] . '&page=1" class="category-link ' . ($is_active ? 'active' : '') . '">';
            $html .= '<span class="category-name"><i class="fas fa-folder"></i> ' . htmlspecialchars($cat['name']) . '</span>';
            $html .= '</a>';
            $html .= '</div>';
        }
        $html .= '</li>';
    }
    return $html;
}

$category_list_html = displayCategoryTree($category_tree, $category_id);

// Check if user is logged in
$is_logged_in = isset($_SESSION['user_id']) && $_SESSION['role'] == 'customer';

// Initialize compare array if not exists
if (!isset($_SESSION['compare_products'])) {
    $_SESSION['compare_products'] = [];
}

include '../../includes/navbar.php';
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop - UNK System</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body { font-family: 'Inter', sans-serif; background: #f7f9fc; color: #1e293b; }
        
        /* Main Content Area */
        .customer-content {
            margin-left: 2px;
            padding: 30px 35px;
            min-height: 100vh;
            background: #f5f7fb;
            transition: all 0.3s ease;
        }
        
        /* Hero Section */
        .hero-section {
            border-radius: 24px;
            padding: 20px 0px;
            margin-bottom: 35px;
            color: #1e293b;
        }
        
        .hero-section h1 {
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 8px;
        }
        
        .hero-section h1 i {
            color: #e67e22;
            margin-right: 12px;
        }
        
        .hero-section p {
            font-size: 14px;
            color: #64748b;
        }
        
        /* Compare Bar */
        .compare-bar {
            background: white;
            border-radius: 16px;
            padding: 10px 20px;
            margin-bottom: 25px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
            border: 1px solid #e2e8f0;
        }
        .compare-count {
            font-size: 13px;
            color: #64748b;
        }
        .compare-count strong {
            color: #e67e22;
        }
        .btn-compare-page {
            background: #3498db;
            color: white;
            padding: 8px 16px;
            border-radius: 10px;
            text-decoration: none;
            font-size: 12px;
        }
        
        /* Mobile Toggle Button */
        .filter-toggle-btn {
            display: none;
            background: #e67e22;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 40px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            margin-bottom: 20px;
            width: 100%;
            text-align: center;
        }
        
        /* Shop Layout */
        .shop-layout {
            display: grid;
            grid-template-columns: 280px 1fr;
            gap: 30px;
        }
        
        /* Sidebar Filters */
        .filters-sidebar {
            position: sticky;
            top: 20px;
            height: fit-content;
            transition: transform 0.3s ease;
        }
        
        .filter-card {
            background: white;
            border-radius: 20px;
            padding: 20px;
            margin-bottom: 25px;
            border: 1px solid #e2e8f0;
        }
        
        .filter-title {
            font-size: 15px;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e67e22;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .filter-title i {
            color: #e67e22;
        }
        
        .search-input {
            width: 100%;
            padding: 10px 14px;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            font-size: 13px;
        }
        
        .search-input:focus {
            outline: none;
            border-color: #e67e22;
        }
        
        /* Category styles */
        .category-list {
            list-style: none;
            margin: 0;
            padding: 0;
        }
        
        .category-item {
            margin-bottom: 4px;
        }
        
        .category-link-wrapper {
            display: block;
        }
        
        .category-link {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 8px 12px;
            background: #f8fafc;
            border-radius: 10px;
            text-decoration: none;
            color: #475569;
            transition: all 0.3s;
            font-size: 12px;
            cursor: pointer;
        }
        
        .category-link.main-category {
            cursor: pointer;
        }
        
        .category-link:hover {
            background: #e67e22;
            color: white;
        }
        
        .category-link.active {
            background: #e67e22;
            color: white;
        }
        
        .category-name {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .category-toggle-icon {
            margin-left: 8px;
            font-size: 12px;
            color: #e67e22;
        }
        
        .subcategory-list {
            list-style: none;
            margin-left: 28px;
            padding-left: 10px;
            border-left: 1px dashed #e2e8f0;
        }
        
        .category-count {
            background: rgba(0,0,0,0.08);
            padding: 2px 6px;
            border-radius: 30px;
            font-size: 10px;
        }
        
        /* Price inputs etc remain same */
        .price-inputs {
            display: flex;
            gap: 10px;
            margin-bottom: 12px;
        }
        
        .price-field {
            flex: 1;
        }
        
        .price-field label {
            display: block;
            font-size: 10px;
            font-weight: 600;
            color: #64748b;
            margin-bottom: 5px;
        }
        
        .price-field input {
            width: 100%;
            padding: 8px 10px;
            border: 2px solid #e2e8f0;
            border-radius: 10px;
            font-size: 12px;
        }
        
        .btn {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 10px;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            text-align: center;
            border: none;
        }
        
        .btn-primary {
            background: #e67e22;
            color: white;
        }
        
        .btn-primary:hover {
            background: #d35400;
            transform: translateY(-2px);
        }
        
        .btn-outline {
            background: transparent;
            border: 2px solid #e2e8f0;
            color: #475569;
        }
        
        .btn-outline:hover {
            border-color: #e67e22;
            color: #e67e22;
        }
        
        .btn-block {
            width: 100%;
        }
        
        /* Products Header */
        .products-header {
            background: white;
            border-radius: 16px;
            padding: 14px 20px;
            margin-bottom: 25px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 12px;
            border: 1px solid #e2e8f0;
        }
        
        .results-count {
            color: #64748b;
            font-size: 12px;
        }
        
        .results-count strong {
            color: #e67e22;
        }
        
        .sort-select {
            padding: 8px 16px;
            border: 2px solid #e2e8f0;
            border-radius: 30px;
            background: white;
            font-size: 12px;
            cursor: pointer;
        }
        
        /* Products Grid */
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
        }
        
        .product-card {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            transition: all 0.3s;
            border: 1px solid #e2e8f0;
            position: relative;
        }
        
        .product-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px -8px rgba(0,0,0,0.1);
            border-color: #e67e22;
        }
        
        .product-badges {
            position: absolute;
            top: 10px;
            left: 10px;
            z-index: 10;
            display: flex;
            gap: 5px;
        }
        
        .badge {
            padding: 3px 8px;
            border-radius: 20px;
            font-size: 9px;
            font-weight: 700;
        }
        
        .badge-new {
            background: #27ae60;
            color: white;
        }
        
        .badge-low-stock {
            background: #f39c12;
            color: white;
        }
        
        .product-image {
            position: relative;
            height: 160px;
            overflow: hidden;
            background: #f8fafc;
        }
        
        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }
        
        .product-card:hover .product-image img {
            transform: scale(1.05);
        }
        
        .product-actions-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(transparent, rgba(0,0,0,0.7));
            padding: 10px;
            display: flex;
            gap: 8px;
            justify-content: center;
            transform: translateY(100%);
            transition: transform 0.3s;
        }
        
        .product-card:hover .product-actions-overlay {
            transform: translateY(0);
        }
        
        .action-icon {
            background: white;
            width: 32px;
            height: 32px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s;
            border: none;
        }
        
        .action-icon:hover {
            background: #e67e22;
            color: white;
        }
        
        .product-info {
            padding: 12px;
        }
        
        .product-category {
            font-size: 9px;
            font-weight: 600;
            color: #e67e22;
            text-transform: uppercase;
            margin-bottom: 5px;
        }
        
        .product-title {
            font-size: 13px;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 5px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            height: 34px;
        }
        
        .product-business {
            font-size: 10px;
            color: #64748b;
            margin-bottom: 6px;
            display: flex;
            align-items: center;
            gap: 4px;
        }
        
        .product-price {
            font-size: 16px;
            font-weight: 800;
            color: #e67e22;
            margin-bottom: 10px;
        }
        
        .product-actions {
            display: flex;
            gap: 6px;
            margin-top: 8px;
        }
        
        .btn-cart {
            flex: 2;
            padding: 6px 8px;
            background: #2c3e50;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 10px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 4px;
        }
        
        .btn-cart:hover {
            background: #e67e22;
        }
        
        .btn-view {
            flex: 1;
            padding: 6px 8px;
            background: #f8fafc;
            color: #1e293b;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            font-size: 10px;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 4px;
        }
        
        .btn-view:hover {
            background: #e67e22;
            color: white;
            border-color: #e67e22;
        }
        
        /* Pagination */
        .pagination {
            display: flex;
            justify-content: center;
            gap: 8px;
            margin-top: 40px;
            margin-bottom: 20px;
        }
        
        .pagination a, .pagination span {
            padding: 8px 14px;
            border-radius: 10px;
            text-decoration: none;
            font-size: 13px;
            font-weight: 500;
            transition: all 0.3s;
        }
        
        .pagination a {
            background: white;
            color: #475569;
            border: 1px solid #e2e8f0;
        }
        
        .pagination a:hover {
            background: #e67e22;
            color: white;
        }
        
        .pagination .active {
            background: #e67e22;
            color: white;
        }
        
        .empty-state {
            text-align: center;
            padding: 60px 40px;
            background: white;
            border-radius: 20px;
        }
        
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.7);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }
        .modal.active {
            display: flex;
        }
        .modal-content {
            max-width: 800px;
            width: 90%;
            max-height: 90%;
            overflow: auto;
            background: white;
            border-radius: 20px;
        }
        
        .toast {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: #27ae60;
            color: white;
            padding: 12px 20px;
            border-radius: 10px;
            z-index: 1001;
            display: none;
            animation: slideIn 0.3s ease;
        }
        @keyframes slideIn {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        
        /* Responsive */
        @media (max-width: 1024px) {
            .customer-content {
                margin-left: 0;
                padding: 20px;
            }
            
            .filter-toggle-btn {
                display: block;
            }
            
            .shop-layout {
                grid-template-columns: 1fr;
            }
            
            .filters-sidebar {
                position: fixed;
                top: 0;
                left: -280px;
                width: 280px;
                height: 100vh;
                overflow-y: auto;
                background: #f5f7fb;
                z-index: 1002;
                padding: 20px;
                transition: left 0.3s ease;
                box-shadow: 2px 0 10px rgba(0,0,0,0.1);
            }
            
            .filters-sidebar.show {
                left: 0;
            }
            
            /* Overlay when sidebar is open on mobile */
            .sidebar-overlay {
                display: none;
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: rgba(0,0,0,0.5);
                z-index: 1001;
            }
            .sidebar-overlay.show {
                display: block;
            }
        }
        
        @media (max-width: 768px) {
            .products-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 12px;
            }
            .product-image {
                height: 120px;
            }
            .product-title {
                font-size: 11px;
                height: 28px;
            }
            .product-price {
                font-size: 13px;
            }
        }
        
        @media (max-width: 480px) {
            .customer-content {
                padding: 15px;
            }
            .products-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 10px;
            }
        }
    </style>
</head>
<body>

<div class="customer-content">
    <!-- Hero Section -->
    <div class="hero-section">
        <h1><i class="fas fa-store"></i> Welcome to Our Shop</h1>
        <p>Discover amazing products from trusted sellers across Tanzania</p>
    </div>
    
    <!-- Compare Bar -->
    <div class="compare-bar">
        <div class="compare-count">
            <i class="fas fa-chart-line"></i> 
            <strong><?php echo count($_SESSION['compare_products']); ?></strong> / 4 products selected for comparison
        </div>
        <div>
            <?php if (!empty($_SESSION['compare_products'])): ?>
                <a href="compare.php" class="btn-compare-page"><i class="fas fa-chart-line"></i> Compare Now</a>
            <?php endif; ?>
        </div>
    </div>
    
    <!-- Mobile Filter Toggle Button -->
    <button class="filter-toggle-btn" id="filterToggleBtn">
        <i class="fas fa-filter"></i> Show Filters
    </button>
    
    <!-- Sidebar Overlay (for mobile) -->
    <div class="sidebar-overlay" id="sidebarOverlay" onclick="closeSidebar()"></div>
    
    <div class="shop-layout">
        <!-- Sidebar Filters -->
        <aside class="filters-sidebar" id="filtersSidebar">
            <div class="filter-card">
                <div class="filter-title"><i class="fas fa-search"></i> Search Products</div>
                <form method="GET">
                    <input type="text" name="search" class="search-input" placeholder="What are you looking for?" value="<?php echo htmlspecialchars($search); ?>">
                    <button type="submit" class="btn btn-primary btn-block" style="margin-top: 12px;"><i class="fas fa-search"></i> Search</button>
                </form>
            </div>
            
            <div class="filter-card">
                <div class="filter-title"><i class="fas fa-tags"></i> Categories</div>
                <ul class="category-list">
                    <li class="category-item">
                        <div class="category-link-wrapper">
                            <a href="?category=0&page=1" class="category-link <?php echo $category_id == 0 ? 'active' : ''; ?>">
                                <span class="category-name"><i class="fas fa-th-large"></i> All Products</span>
                            </a>
                        </div>
                    </li>
                    <?php echo $category_list_html; ?>
                </ul>
            </div>
            
            <div class="filter-card">
                <div class="filter-title"><i class="fas fa-dollar-sign"></i> Price Range</div>
                <form method="GET">
                    <div class="price-inputs">
                        <div class="price-field">
                            <label>Min (TSh)</label>
                            <input type="number" name="min_price" placeholder="0" value="<?php echo $min_price ?: ''; ?>">
                        </div>
                        <div class="price-field">
                            <label>Max (TSh)</label>
                            <input type="number" name="max_price" placeholder="Any" value="<?php echo $max_price ?: ''; ?>">
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary btn-block">Apply Price</button>
                </form>
            </div>
            
            <div class="filter-card">
                <a href="index.php" class="btn btn-outline btn-block"><i class="fas fa-times"></i> Clear All Filters</a>
            </div>
            
            <div class="filter-card">
                <div class="filter-title"><i class="fas fa-history"></i> Recently Viewed</div>
                <a href="recently-viewed.php" class="btn btn-outline btn-block">View History →</a>
            </div>
            
            <div class="filter-card">
                <div class="filter-title"><i class="fas fa-bell"></i> Price Alerts</div>
                <a href="price-alert.php" class="btn btn-outline btn-block">Manage Alerts →</a>
            </div>
        </aside>
        
        <!-- Products Main Content -->
        <main>
            <div class="products-header">
                <div class="results-count">
                    <i class="fas fa-box"></i> Showing <strong><?php echo count($products); ?></strong> of <strong><?php echo $total_products; ?></strong> products
                </div>
                <form method="GET">
                    <select name="sort" class="sort-select" onchange="this.form.submit()">
                        <option value="newest" <?php echo $sort == 'newest' ? 'selected' : ''; ?>>📅 Newest First</option>
                        <option value="price_low" <?php echo $sort == 'price_low' ? 'selected' : ''; ?>>💰 Price: Low to High</option>
                        <option value="price_high" <?php echo $sort == 'price_high' ? 'selected' : ''; ?>>💰 Price: High to Low</option>
                        <option value="popular" <?php echo $sort == 'popular' ? 'selected' : ''; ?>>⭐ Most Popular</option>
                        <option value="name_asc" <?php echo $sort == 'name_asc' ? 'selected' : ''; ?>>🔤 Name: A to Z</option>
                        <option value="name_desc" <?php echo $sort == 'name_desc' ? 'selected' : ''; ?>>🔤 Name: Z to A</option>
                    </select>
                </form>
            </div>
            
            <?php if (empty($products)): ?>
                <div class="empty-state">
                    <i class="fas fa-box-open" style="font-size: 64px; color: #cbd5e1;"></i>
                    <h3 style="margin-top: 15px;">No products found</h3>
                    <p>Try adjusting your search or filter to find what you're looking for.</p>
                    <a href="index.php" class="btn btn-primary" style="margin-top: 15px;">Clear Filters</a>
                </div>
            <?php else: ?>
                <div class="products-grid">
                    <?php foreach ($products as $product): 
                        $days_old = (time() - strtotime($product['created_at'])) / (60 * 60 * 24);
                        $img_src = '../../assets/images/default-product.jpg';
                        if (!empty($product['image_url'])) {
                            if (file_exists('../../' . $product['image_url'])) {
                                $img_src = '../../' . $product['image_url'];
                            } elseif (file_exists($product['image_url'])) {
                                $img_src = $product['image_url'];
                            }
                        }
                    ?>
                    <div class="product-card">
                        <div class="product-badges">
                            <?php if ($days_old < 7): ?>
                                <span class="badge badge-new">🔥 New</span>
                            <?php endif; ?>
                            <?php if ($product['quantity_in_stock'] < 10 && $product['quantity_in_stock'] > 0): ?>
                                <span class="badge badge-low-stock">⚠️ Low Stock</span>
                            <?php endif; ?>
                        </div>
                        
                        <div class="product-image">
                            <img src="<?php echo $img_src; ?>" alt="<?php echo htmlspecialchars($product['name']); ?>" onerror="this.src='../../assets/images/default-product.jpg'">
                            <div class="product-actions-overlay">
                                <button class="action-icon" onclick="quickView(<?php echo $product['product_id']; ?>)" title="Quick View"><i class="fas fa-eye"></i></button>
                                <button class="action-icon" onclick="addToCompare(<?php echo $product['product_id']; ?>)" title="Compare"><i class="fas fa-chart-line"></i></button>
                                <button class="action-icon" onclick="setPriceAlert(<?php echo $product['product_id']; ?>, <?php echo $product['price']; ?>)" title="Price Alert"><i class="fas fa-bell"></i></button>
                                <button class="action-icon" onclick="bulkOrder(<?php echo $product['product_id']; ?>)" title="Bulk Order"><i class="fas fa-boxes"></i></button>
                            </div>
                        </div>
                        
                        <div class="product-info">
                            <div class="product-category"><?php echo htmlspecialchars($product['category_name'] ?? 'Product'); ?></div>
                            <h3 class="product-title"><?php echo htmlspecialchars($product['name']); ?></h3>
                            <div class="product-business">
                                <i class="fas fa-store"></i> <?php echo htmlspecialchars($product['business_name']); ?>
                            </div>
                            <div class="product-price">
                                TSh <?php echo number_format($product['price'], 0, '.', ','); ?>
                            </div>
                            
                            <div class="product-actions">
                                <?php if ($is_logged_in && $product['quantity_in_stock'] > 0): ?>
                                    <button class="btn-cart" onclick="addToCart(<?php echo $product['product_id']; ?>)">
                                        <i class="fas fa-cart-plus"></i> Cart
                                    </button>
                                <?php elseif (!$is_logged_in): ?>
                                    <a href="../login.php" class="btn-cart"><i class="fas fa-sign-in-alt"></i> Login</a>
                                <?php else: ?>
                                    <button class="btn-cart" disabled style="background: #94a3b8; cursor: not-allowed;"><i class="fas fa-times"></i> Out of Stock</button>
                                <?php endif; ?>
                                
                                <a href="details.php?id=<?php echo $product['product_id']; ?>" class="btn-view">
                                    <i class="fas fa-eye"></i> View
                                </a>
                            </div>
                        </div>
                    </div>
                    <?php endforeach; ?>
                </div>
                
                <?php if ($total_pages > 1): ?>
                <div class="pagination">
                    <?php if ($page > 1): ?>
                        <a href="?page=<?php echo $page - 1; ?>"><i class="fas fa-chevron-left"></i> Previous</a>
                    <?php else: ?>
                        <span style="background:#f1f5f9; color:#94a3b8;"><i class="fas fa-chevron-left"></i> Previous</span>
                    <?php endif; ?>
                    
                    <?php for ($i = max(1, $page - 2); $i <= min($total_pages, $page + 2); $i++): ?>
                        <?php if ($i == $page): ?>
                            <span class="active"><?php echo $i; ?></span>
                        <?php else: ?>
                            <a href="?page=<?php echo $i; ?>"><?php echo $i; ?></a>
                        <?php endif; ?>
                    <?php endfor; ?>
                    
                    <?php if ($page < $total_pages): ?>
                        <a href="?page=<?php echo $page + 1; ?>">Next <i class="fas fa-chevron-right"></i></a>
                    <?php else: ?>
                        <span style="background:#f1f5f9; color:#94a3b8;">Next <i class="fas fa-chevron-right"></i></span>
                    <?php endif; ?>
                </div>
                <?php endif; ?>
            <?php endif; ?>
        </main>
    </div>
</div>

<!-- Quick View Modal -->
<div id="quickViewModal" class="modal">
    <div class="modal-content" id="quickViewContent"></div>
</div>

<!-- Toast Notification -->
<div id="toast" class="toast"></div>

<script>
function addToCart(productId) {
    let quantity = 1;
    let form = document.createElement('form');
    form.method = 'POST';
    form.action = '../cart/add.php';
    let input1 = document.createElement('input');
    input1.type = 'hidden';
    input1.name = 'product_id';
    input1.value = productId;
    let input2 = document.createElement('input');
    input2.type = 'hidden';
    input2.name = 'quantity';
    input2.value = quantity;
    form.appendChild(input1);
    form.appendChild(input2);
    document.body.appendChild(form);
    form.submit();
}

function quickView(productId) {
    let modal = document.getElementById('quickViewModal');
    let content = document.getElementById('quickViewContent');
    modal.classList.add('active');
    fetch('quick-view.php?id=' + productId)
        .then(response => response.text())
        .then(data => { content.innerHTML = data; });
}

function addToCompare(productId) {
    fetch('compare.php?add=' + productId)
        .then(() => {
            showToast('Product added to comparison!');
            setTimeout(() => { location.reload(); }, 1000);
        });
}

function setPriceAlert(productId, currentPrice) {
    let desiredPrice = prompt('Enter desired price (TSh):\nCurrent price: TSh ' + currentPrice.toLocaleString(), currentPrice * 0.8);
    if (desiredPrice && desiredPrice > 0) {
        let form = document.createElement('form');
        form.method = 'POST';
        form.action = 'price-alert.php';
        let input1 = document.createElement('input');
        input1.type = 'hidden';
        input1.name = 'product_id';
        input1.value = productId;
        let input2 = document.createElement('input');
        input2.type = 'hidden';
        input2.name = 'desired_price';
        input2.value = desiredPrice;
        form.appendChild(input1);
        form.appendChild(input2);
        document.body.appendChild(form);
        form.submit();
    }
}

function bulkOrder(productId) {
    window.location.href = 'bulk-order.php?id=' + productId;
}

function showToast(message) {
    let toast = document.getElementById('toast');
    toast.innerHTML = '<i class="fas fa-check-circle"></i> ' + message;
    toast.style.display = 'block';
    setTimeout(() => { toast.style.display = 'none'; }, 3000);
}

document.getElementById('quickViewModal').addEventListener('click', function(e) {
    if (e.target === this) {
        this.classList.remove('active');
    }
});

// Close modal with Escape key
document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
        document.getElementById('quickViewModal').classList.remove('active');
    }
});

// CATEGORY TOGGLE FUNCTION (click on main category name toggles subcategories, static > icon)
function toggleSubcategories(linkElement) {
    var parentLi = linkElement.closest('.category-item');
    var subUl = parentLi.querySelector('.subcategory-list');
    
    if (subUl) {
        if (subUl.style.display === 'none' || subUl.style.display === '') {
            subUl.style.display = 'block';
        } else {
            subUl.style.display = 'none';
        }
    }
}

// Mobile sidebar toggle
const filterToggleBtn = document.getElementById('filterToggleBtn');
const filtersSidebar = document.getElementById('filtersSidebar');
const sidebarOverlay = document.getElementById('sidebarOverlay');

function openSidebar() {
    filtersSidebar.classList.add('show');
    sidebarOverlay.classList.add('show');
    filterToggleBtn.innerHTML = '<i class="fas fa-times"></i> Hide Filters';
}

function closeSidebar() {
    filtersSidebar.classList.remove('show');
    sidebarOverlay.classList.remove('show');
    filterToggleBtn.innerHTML = '<i class="fas fa-filter"></i> Show Filters';
}

if (filterToggleBtn) {
    filterToggleBtn.addEventListener('click', function() {
        if (filtersSidebar.classList.contains('show')) {
            closeSidebar();
        } else {
            openSidebar();
        }
    });
}
</script>

</body>
</html>

<?php include '../../includes/footer.php'; ?>
